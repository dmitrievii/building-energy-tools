# WEB-0.3 direct dependency baseline (Python 3.12).
# Major scientific-stack upgrades are intentionally deferred to a dedicated validation stage.
# Pillow is pinned directly because Climate 0.7.3 exports looping ground-temperature GIFs.
# Deployment refresh marker 2026-09-24: force a Community Cloud dependency-surface rebuild; package versions unchanged.
streamlit==1.63.0
streamlit-folium==0.27.4
folium==0.20.0
pandas==2.3.3
numpy==1.26.4
plotly==5.24.1
psychrolib==2.5.0
pvlib==0.15.2
requests==2.34.2
beautifulsoup4==4.15.0
openpyxl==3.1.5
pillow==12.3.0
