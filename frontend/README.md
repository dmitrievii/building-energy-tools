# Frontend

Frontend for Building Energy Tools.

Planned stack: Next.js, TypeScript, Tailwind CSS, MDX-ready content structure.

The frontend does not contain engineering calculation logic. It collects inputs, sends requests to the Python backend API, and displays results, warnings, assumptions, and limitations.
